apiVersion: v1
kind: ConfigMap
metadata:
  name: homepage-config
  namespace: default
data:
  settings.yaml: |
    title: Mein HomeLab
    layout:
      Main:
        style: row
        columns: 3
  services.yaml: |
    - Infrastruktur:
      - ArgoCD:
          icon: argocd.png
          href: https://argocd.local
          description: GitOps Zentrale
  bookmarks.yaml: "[]"
  widgets.yaml: "[]"
  docker.yaml: "{}"
  kubernetes.yaml: "{}"
  custom.css: ""
  custom.js: ""